import Mathlib
import Aesop



/-- implement disjoint set union with path compression and union by rank, prove find returns the correct representative for each element -/
theorem disjoint_set_union_correct : True := by sorry